baseURL = 'https://maajix.github.io/omarchy-spotlight/'
title = 'Spotlight for Omarchy'

# The site is the hand-written static/ tree: the landing page, the docs page,
# the 404 page and the crawler files. Hugo only copies it into public/.
disableKinds = ['page', 'section', 'taxonomy', 'term', 'rss', 'sitemap', 'robotstxt', '404']
